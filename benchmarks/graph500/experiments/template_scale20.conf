# ==============================================================================
# GRAPH500 EXPERIMENT CONFIGURATION (V3.0.0)
# ==============================================================================

# Load Cluster Environment
source "./benchmarks/configs/hamilton.cfg"

# Binaries
SAMPI_BUILD_DIR=$(realpath "./build")
GRAPH500_EXEC=$(realpath "./graph500/src/graph500_reference_bfs_sssp")

# Graph500 params
SCALE=20
EDGEFACTOR=16

# Slurm Parameters
NUM_NODES=2
TASKS_PER_NODE=64
TIME_LIMIT="01:00:00"
